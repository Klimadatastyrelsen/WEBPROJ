apiVersion: v1
kind: Service
metadata:
  name: {{ include "webproj.fullname" . }}
  labels:
    {{- include "webproj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "webproj.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
